---
description: プロジェクトの現在の状態を確認し、次にやるべきことを案内
disable-model-invocation: true
allowed-tools: Bash, Read, Glob, Grep
---

# プロジェクトステータス

## チェック項目

### 環境

- `.env.local` が存在するか
- `CMX_API_KEY` が設定されているか

### サイトコンフィグ

- `cmx/site-config.md` が記入済みか（「未設定」が残っていないか）

### スキーマ

- `src/lib/constants/collections.ts` の登録スラッグ一覧
- `cmx/generated/` の生成済みファイル一覧
- 両者の不一致がないか

### ページ

- `src/app/` 配下のページ一覧
- 登録済みコレクションに対応するページが全て存在するか
- データタイプに対応するページが存在するか

### コンポーネント

- `cmx/components/` の JSON 定義一覧
- `src/components/custom/index.ts` のエクスポート一覧
- 定義と実装の不一致がないか

## 出力フォーマット

```
## プロジェクトステータス

環境: ✓ 設定済み / ✗ 未設定
コンフィグ: ✓ 記入済み / △ 一部未設定 / ✗ 未作成
スキーマ: コレクション {n} 件 / データタイプ {n} 件
ページ: {n} 件実装済み / {n} 件未実装
コンポーネント: {n} 件（定義 {n} / 実装 {n}）

### 推奨アクション
- {状態に応じた次のステップとコマンドを案内}
```
