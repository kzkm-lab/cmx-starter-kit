{
  "name": "cmx-starter-kit",
  "version": "0.1.0",
  "private": true,
  "description": "CMX Starter Kit - A Next.js template for building content-driven websites with CMX",
  "scripts": {
    "dev": "pnpm --filter site dev & pnpm --filter dev dev",
    "dev:site": "pnpm --filter site dev",
    "dev:setup": "pnpm --filter dev dev",
    "build": "pnpm --filter site build",
    "build:cf": "pnpm --filter site build:cf",
    "deploy": "pnpm --filter site deploy",
    "typecheck": "pnpm --filter site typecheck",
    "lint": "pnpm --filter site lint"
  }
}
